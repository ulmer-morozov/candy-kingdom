import { generatedMarcyCmsTypes } from './generated-marcy-cms-types';

describe('generatedMarcyCmsTypes', () => {
  it('should work', () => {
    expect(generatedMarcyCmsTypes()).toEqual('generated-marcy-cms-types');
  });
});
